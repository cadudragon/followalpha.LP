namespace FollowAlpha.LP.Application;

/// <summary>
/// Anchor type so tests can reach this assembly via <c>typeof(AssemblyReference).Assembly</c>.
/// Carries no behaviour. Use cases and ports land from Phase 2 onwards.
/// </summary>
public sealed class AssemblyReference;
